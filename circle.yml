dependencies:
    override:
        - sudo pip install awscli
deployment:
  prod:
    branch: master
    commands:
      - aws s3 sync /home/ubuntu/example-docker-application-pray https://s3.amazonaws.com/example-docker-application-pray --delete
